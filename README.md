Run build for first time docker
========================
`docker-compose up --build`


Run docker start in background
====================================
`docker-compose up -d`


Install composer reqs
========================
`docker-compose run dashboard composer install --ignore-platform-reqs`